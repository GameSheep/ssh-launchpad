import { useState, type FormEvent } from 'react'
import type { RemoteAppInput, RemoteAppRecord, ServerRecord } from '@ssh-launchpad/shared'

function initial(serverId: string): RemoteAppInput { return { serverId, name: '', type: 'dsh', remoteHost: '127.0.0.1', remotePort: 3080, localPort: 13080, protocol: 'http', healthPath: '/', autoStart: true, workingDirectory: '', startCommand: '', stopOnDisconnect: false, stopCommand: '', iconKind: 'letter', iconValue: 'DS', startTimeoutMs: 30000, healthTimeoutMs: 10000 } }

export function AppDialog({ servers, editing, onClose, onSave }: { servers: ServerRecord[]; editing?: RemoteAppRecord; onClose(): void; onSave(input: RemoteAppInput): Promise<void> }) {
  const [value, setValue] = useState<RemoteAppInput>(() => editing ? { ...editing } : initial(servers[0]?.id ?? '')); const [saving, setSaving] = useState(false)
  const update = <K extends keyof RemoteAppInput>(key: K, next: RemoteAppInput[K]) => setValue((current) => ({ ...current, [key]: next }))
  const submit = async (event: FormEvent) => { event.preventDefault(); setSaving(true); try { await onSave(value); onClose() } catch { /* parent displays */ } finally { setSaving(false) } }
  return <div className="modal-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose() }}><form className="modal wide-modal" onSubmit={submit}><header className="modal-header"><div><span className="eyebrow">远程服务</span><h2>{editing ? '编辑应用' : '添加应用'}</h2></div><button type="button" className="modal-close" onClick={onClose}>×</button></header><div className="modal-body"><div className="form-grid"><label>所属服务器<select required value={value.serverId} onChange={(event) => update('serverId', event.target.value)}>{servers.map((server) => <option key={server.id} value={server.id}>{server.name} · {server.host}</option>)}</select></label><label>应用名称<input required value={value.name} onChange={(event) => update('name', event.target.value)} placeholder="DeepSeek Harness" /></label><label>应用类型<select value={value.type} onChange={(event) => update('type', event.target.value as RemoteAppInput['type'])}><option value="dsh">DeepSeek Harness</option><option value="openclaw">OpenClaw</option><option value="custom">自定义</option></select></label><label>图标文字<input maxLength={3} value={value.iconValue} onChange={(event) => update('iconValue', event.target.value)} placeholder="DS" /></label><label>远端地址<input required value={value.remoteHost} onChange={(event) => update('remoteHost', event.target.value)} /></label><label>远端端口<input required type="number" min="1" max="65535" value={value.remotePort} onChange={(event) => update('remotePort', Number(event.target.value))} /></label><label>本地端口<input required type="number" min="1" max="65535" value={value.localPort} onChange={(event) => update('localPort', Number(event.target.value))} /><small>全局唯一，冲突时不会自动改号</small></label><label>访问协议<select value={value.protocol} onChange={(event) => update('protocol', event.target.value as RemoteAppInput['protocol'])}><option value="http">HTTP</option><option value="https">HTTPS</option></select></label><label className="wide">健康检查路径<input required value={value.healthPath} onChange={(event) => update('healthPath', event.target.value)} /></label><label className="wide check-row"><input type="checkbox" checked={value.autoStart} onChange={(event) => update('autoStart', event.target.checked)} /><span>连接时自动启动远端程序</span></label>{value.autoStart && <label className="wide">启动命令<input required value={value.startCommand ?? ''} onChange={(event) => update('startCommand', event.target.value)} placeholder="python -m deepseek_harness --port 3080" /></label>}<label className="wide">工作目录<input value={value.workingDirectory ?? ''} onChange={(event) => update('workingDirectory', event.target.value)} placeholder="/opt/deepseek-harness" /></label><label className="wide check-row"><input type="checkbox" checked={value.stopOnDisconnect} onChange={(event) => update('stopOnDisconnect', event.target.checked)} /><span>断开时执行显式停止命令</span></label>{value.stopOnDisconnect && <label className="wide">停止命令<input required value={value.stopCommand ?? ''} onChange={(event) => update('stopCommand', event.target.value)} placeholder="docker compose stop" /></label>}</div></div><footer className="modal-footer"><span className="security-note">⚠ 启动命令将在远程服务器执行</span><button type="button" className="ghost" onClick={onClose}>取消</button><button className="primary" disabled={saving || !servers.length}>{saving ? '保存中…' : editing ? '保存修改' : '添加应用'}</button></footer></form></div>
}
